import torch

from StrataNet.vad import StrataVAD
from dataset import BookSummaryDataset


epochs = 1
lr = 1e-3
n_levels = latent_dim, d_model,
                 nhead, num_layers, dim_feedforward, dropout


def train():
    model = StrataVAD(n_l)

if __name__ == "__main__":
    train()
